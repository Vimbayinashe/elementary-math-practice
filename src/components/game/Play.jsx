import React from 'react';

const Play = ({ gameMethod, level }) => {

    console.log('Play: ', gameMethod, level);
    

    return(
        <section>
            <h2>Play</h2>
        </section>
    )
}

export default Play;